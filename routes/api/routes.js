const authentRouter = require('./authent-routes');
const calendarRouter = require('./calendar-routes');
const eventRouter = require('./event-routes');
const userRouter = require('./user-routes');

module.exports = {
    authentRouter,
    calendarRouter,
    eventRouter,
    userRouter
}
